# Issue #69 — folder-scoped operations documentation

This OpenSpec change rewrites current public onboarding around the folder-native
execution and output contracts delivered by Issues #64 and #65. It documents
portable logical input roles, one explicit output folder, target selection,
internet policy, isolation limits, reproducibility, and a synthetic first-hour
flow without requiring a case-management product or adapter.
