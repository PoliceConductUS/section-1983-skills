# Issue #59 implementation plan

## Goal

Add a bounded police-policy source-collection skill that produces ordinary files
and domain YAML through the existing folder/output boundaries.

## Steps

1. Correct the live issue and establish the stacked OpenSpec change.
2. RED-test the exact four-role contract, authorized-internet boundary, and
   absence of package/graph concepts.
3. Implement the installed skill, source-documentation contract, and strict
   deterministic record validator.
4. RED-test fictional version, adoption, classification, coverage-gap,
   provenance, mutation, output, temp, and collector/analyzer cases.
5. Implement deterministic output-relative artifact plans for trusted-host
   publication.
6. Run focused and full validation, review the whole story, archive OpenSpec,
   verify the exact remote head, and mark the PR ready while leaving PR and
   issue open.
