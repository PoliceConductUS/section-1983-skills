# Retrospective

## What changed

Issue #60 now provides a public authorized collector over six exact recursive
read-only folders. It returns ordinary legal-authority source files, adjacent
strict domain YAML, an ordered candidate index, and an explicit search-gap
register for later independent authority audit.

## What verification caught

Shared folder-governance tests found that the new skill varied two required
portable phrases: the host-enforcement stop sentence and the singular
`folder-relative path` wording. Aligning those phrases preserved behavior and
made installed guidance mechanically consistent.

Whole-story review then found that initial plan construction rejected duplicate
retrieval-result identities without declared duplicate relationships, but plan
re-validation did not. A failing tamper test now proves that both construction
and validation apply the same duplicate-identity rule.

## Result

Authority collection uses only caller-declared folders, ordinary files, domain
YAML, and expressly authorized internet access. It has no package or graph
abstraction, cannot acquire filesystem or output authority, and never
substitutes collection for verified text, quotations, pinpoints, status, binding
force, treatment, proposition fit, or fair-warning analysis.
