# Retrospective

## What remained valid

The generic builder, source classes, attribution rules, comparison discipline,
neutral-transfer gate, anti-prediction boundary, and removal of the embedded
real-judge skill all survived the persistence correction unchanged.

## What changed

The rejected package layer was removed from the skill surface, operation test,
metadata, public documentation, and specifications. Acquisition and compilation
now compose solely through declared read-only folders and domain YAML source
records.

## Downstream rule

Issue #61 may receive selected immutable bytes from these declared folders. It
must not recreate a profile package, profile loader, graph, or ambient workspace
contract.
