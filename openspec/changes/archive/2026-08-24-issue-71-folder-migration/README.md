# Issue #71 — implemented-skill folder migration

This OpenSpec change migrates every implemented public skill from generic or
project-shaped input/output instructions to an exact install-local folder
contract. It preserves approved legal behavior while binding each operation to
fixed recursive read-only roles, one explicit output folder, a target policy, an
explicit internet policy, and append-immutable publication through the trusted
host's shared output boundary.
