# Filing CI Result

The cached result is stale because it covers `draft-beta`, while the current
draft fingerprint is `draft-alpha`. [cite:CURRENT-DRAFT] [cite:CACHED-RESULT]

# Gate Status

The filing-readiness gate remains open. The stale result establishes nothing
about the current draft's merits.

# Next Action

Run Filing CI against the current draft and evaluate only the resulting report.
