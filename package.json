{
  "private": true,
  "scripts": {
    "format": "prettier --write \"**/*.{json,md,yml,yaml}\"",
    "format:check": "prettier --check \"**/*.{json,md,yml,yaml}\"",
    "test:unit": "python3 -m unittest discover -s skills/section-1983-drafting/scripts -p \"test_*.py\" && npm run test:evaluations",
    "test:evaluations": "python3 -m unittest discover -s evaluations/tests -p \"test_*.py\"",
    "evaluations:corpus": "python3 -m evaluations.cli --corpus evaluations/fixtures --baseline evaluations/baseline.json --json-output evaluations/corpus-report.json --markdown-output evaluations/corpus-report.md",
    "skills:list": "skills add . --list",
    "openspec:status": "openspec status",
    "openspec:validate": "openspec validate --all",
    "validate": "npm run format:check && npm run test:unit && npm run skills:list && npm run openspec:validate && npm run evaluations:corpus"
  },
  "devDependencies": {
    "@fission-ai/openspec": "1.3.1",
    "prettier": "3.8.3",
    "skills": "1.5.23"
  }
}
