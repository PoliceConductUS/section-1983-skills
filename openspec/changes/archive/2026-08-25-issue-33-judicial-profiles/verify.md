# Verification

Verified on 2026-08-25 on the stacked Issue #33 branch after the corrected
direct-output folder-package foundation.

- Focused profile, folder-contract, guide, receipt, package, and governance
  suites passed.
- The final operation-policy correction suite passed 111 tests.
- `npm run validate` passed:
  - formatting passed;
  - 27 drafting unit tests passed;
  - 538 evaluation tests passed;
  - 22 skills were discovered;
  - 26 OpenSpec items passed;
  - corpus evaluation completed; and
  - governance validation passed.

The generic builder validates from a copied install-local skill, acquisition and
compilation publish separate reloadable packages directly beneath their
caller-selected output roots, original inputs remain byte-identical, and
same-run output/input overlap fails closed. The selected operation is bound to
its exact internet policy before root resolution. Every invocation requires the
full absolute output-folder path, and every transient byte is confined to
`<output-folder>/temp` by the shared output boundary.

Current public skills, routes, tests, governance, and active specifications
contain no embedded real-judge package or CaseGraph dependency. Archived
OpenSpec history remains unchanged.
