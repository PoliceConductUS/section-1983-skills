## 1. Contract and validator

- [x] 1.1 Add failing public-seam tests and fixtures for strict version
      rejection, typed individual-capacity and qualified-immunity units, and
      every Monell path type.
- [x] 1.2 Replace the canonical complaint handoff schema and reference with
      contract version 2.
- [x] 1.3 Implement the install-local standard-library validator with separate
      structural and CaseGraph-assessment receipt results.
- [x] 1.4 Add failing tests and fixtures for graph status, fingerprint,
      node/file hash, and exact authority-passage receipt validation.
- [x] 1.5 Implement deterministic assessment-receipt validation without invoking
      or depending on the CaseGraph CLI.

## 2. Monell skills

- [x] 2.1 Add failing behavior evaluations for path-separated planning, temporal
      lanes, recommendations, missing connections, and litigation-principal
      approval boundaries.
- [x] 2.2 Create and validate planning-section-1983-monell-claims against those
      evaluations.
- [x] 2.3 Add failing behavior evaluations for approved-path-only drafting and
      typed complaint deltas.
- [x] 2.4 Create and validate drafting-section-1983-monell-claims against those
      evaluations.
- [x] 2.5 Update the canonical complaint skill routing and handoff instructions
      for the two Monell specializations.

## 3. Filing CI and repository integration

- [x] 3.1 Add failing Filing CI evaluations for drafting mode, filing mode, and
      every missing, invalid, incompatible, stale, partial, and completed
      assessment state.
- [x] 3.2 Integrate the canonical version-2 validator and CaseGraph assessment
      receipt into Filing CI without collapsing structural and assessment
      outcomes.
- [x] 3.3 Update public skill listings, agent metadata, governance provenance,
      and synthetic evaluation corpus coverage.
- [x] 3.4 Run formatting, focused tests, skill discovery, OpenSpec validation,
      evaluation corpus checks, governance validation, and the complete
      repository validation command.

## 4. Completion

- [x] 4.1 Record exact verification evidence and implementation commit
      identifiers in the change artifacts.
- [x] 4.2 Archive the completed OpenSpec change on its owning branch after
      implementation review.
