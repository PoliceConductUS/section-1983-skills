# Issue #32 implementation plan

## Goal

Add one validated municipal-profile folder role to five existing Section 1983
consumers and preserve each consumer's legal, filesystem, network, target, and
output boundaries.

## Steps

1. Correct the live issue, create the stacked worktree, and publish this design
   in a draft PR.
2. RED-test the five exact folder contracts and the four-file profile validation
   boundary.
3. Implement one strict pure validator for Issue #31 output bytes and add the
   install-local consumption instructions to each skill.
4. RED-test consumer-specific complaint, Rule 12, discovery, deposition, and
   adversarial boundaries, including stale/failing inputs and reserved choices.
5. Update governance and public composition guidance, then run focused and full
   validation.
6. Archive OpenSpec, verify the exact remote head and GitHub checks, and mark
   the PR ready while leaving PR and issue open.
