# Retrospective

## What changed

Issue #59 now provides a public folder-native source-collection skill. It
accepts four exact recursive read-only folder roles, performs only expressly
authorized bounded research, and returns ordinary source bytes, adjacent domain
`SOURCE.yaml`, an ordered candidate index, and a bounded gap register for
trusted-host publication.

## What verification caught

The first green implementation documented internet provenance inside YAML but
did not return the shared writer's artifact-level internet source records. The
trusted-host integration test added those records and proved output-local temp
configuration. Whole-story review also closed two plan-tampering gaps:
source-documentation files must remain adjacent to their ordinary files, and
duplicate relationships must resolve inside the same proposed collection.

Full validation then exposed that custom test data had been placed in the
canonical evaluation-corpus namespace and that the new skill lacked two exact
portable folder-guidance phrases. Moving the data into the test-fixture
namespace and completing the install-local wording restored the canonical corpus
and governance gates. A candidate-agent timeout appeared once under the first
full-suite run but did not reproduce independently or on the next full run; no
unrelated behavior was changed.

The first exact-head GitHub run then exposed a local-environment dependency:
PyYAML was globally installed on the development machine but absent from the
clean Python 3.12 runner. Pinning the tested PyYAML version in
`requirements.txt` made the YAML runtime dependency explicit.

## Result

Police-policy collection is independent of packages and graphs. Caller folders
define scope, domain YAML documents ordinary bytes and research coverage, the
collector cannot perform policy analysis in the same invocation, and the
explicit output folder owns every durable and transient write.
