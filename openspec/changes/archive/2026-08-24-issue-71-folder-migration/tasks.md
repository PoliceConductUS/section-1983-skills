# Tasks

## 1. Exact contract matrix RED/GREEN

- [x] 1.1 Add deterministic RED tests and schema fixtures for the exact 22-skill
      contract matrix, isolated installation, strict object shape, role order,
      target policy, internet policy, and append-immutable output mode.
- [x] 1.2 Add `references/folder-contract.json` to every public skill and extend
      governance validation without changing legal instructions.
- [x] 1.3 Prove missing, extra, duplicate, reordered, unsafe, or mismatched
      contract fields fail with stable skill-specific findings.

## 2. Governance and quality-control output migration

- [x] 2.1 Replace version/project/audits report placement with declared-role
      reads and append-immutable explicit-output publication in governance,
      every independently installable QC skill, durable specs, and tests.
- [x] 2.2 Preserve artifact non-mutation, advisory recommendations, separately
      authorized remediation, prior-report exclusion, unique reports, and fresh
      re-verification.
- [x] 2.3 Add filesystem tests proving input hashes remain unchanged and reports
      cannot escape, overwrite, or bypass the shared output boundary.

## 3. Packaged complaint checks and Filing CI

- [x] 3.1 Add RED tests for isolated package installation, required Filing CI
      roles/target, packaged-checker dispatch only, stable unavailable classes,
      and forbidden external command dispatch.
- [x] 3.2 Package the complaint contract's mechanical checks and a narrow Filing
      CI wrapper that accepts only declared input-root/relative-target data and
      emits deterministic results.
- [x] 3.3 Migrate unavailable, stale-result, and hard-finding fixtures while
      preserving the separate drafting/remediation loop and all legal
      exclusions.

## 4. Adversarial-review runtime

- [x] 4.1 Add RED tests for declared filing/source roles, required target,
      authorized provider network, isolated installation, byte emission, and
      absence of project/version/output-path arguments.
- [x] 4.2 Refactor the launcher into an input-confined processor whose host
      adapter publishes the existing categorized report and internet provenance
      through `OutputRun`.
- [x] 4.3 Preserve packet fingerprints, empty provider capabilities, strict
      response schema, bounded failures, credential secrecy, and plaintiff-
      reserved decisions.

## 5. Judge-overlay receipt

- [x] 5.1 Add RED tests replacing project/version/audits arguments with declared
      filing, corpus, and conduct roles plus a required filing target.
- [x] 5.2 Make the packaged helper validate declared relative artifacts and emit
      deterministic receipt bytes without writing or importing root scripts.
- [x] 5.3 Preserve anti-gaming checks, exact fingerprints, supported-change and
      no-change outcomes, immutable inputs, and advisory remediation.

## 6. Overlay, corpus, and linter helpers

- [x] 6.1 Add role/target and isolated-install RED tests for litigation overlay,
      counsel overlay, Rule 59 corpus, and drafting-linter helpers.
- [x] 6.2 Replace arbitrary positional paths with declared input roots and
      canonical relative targets; return deterministic bytes/results only.
- [x] 6.3 Preserve existing schemas, provenance, checked-through dates,
      fingerprints, validation findings, and legal/non-gating boundaries.

## 7. Remaining instruction-only skill families

- [x] 7.1 Add exact role, target, internet, and append-immutable output sections
      to drafting, RRD, discovery, declaration, authority, writing, and overlay
      entrypoints while preserving their approved legal behavior.
- [x] 7.2 Update applicable durable specs and README/current docs to remove
      external checker, project/version, repository-path, CaseGraph, and Git
      runtime requirements without changing historical archives.
- [x] 7.3 Prove each independently installed skill rejects undeclared roles and
      missing required targets and never requires network when disabled.

## 8. Whole-stack verification and archive

- [x] 8.1 Run the exact 22-skill matrix, helper-focused suites, all evaluations,
      governance, strict OpenSpec, current-contract terminology searches, and
      `npm run validate`.
- [x] 8.2 Independently review contract/schema drift, role variants, helper
      purity, input immutability, output confinement, internet enforcement,
      packaged checker scope, and unchanged legal semantics; correct accepted
      Important findings test-first.
- [x] 8.3 Write `verify.md` and `retrospective.md`, archive through the
      repository-local OpenSpec CLI, validate durable specs, commit, push, and
      complete fresh final verification before PR readiness.
